{
  "routes": [
    {
      "method": "GET",
      "path": "/date-events",
      "handler": "date-event.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/date-events/count",
      "handler": "date-event.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/date-events/:id",
      "handler": "date-event.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/date-events",
      "handler": "date-event.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/date-events/:id",
      "handler": "date-event.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/date-events/:id",
      "handler": "date-event.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
